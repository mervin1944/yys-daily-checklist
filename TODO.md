# TODO

## 待做

- [ ] 换掉 `public/icons/` 下的占位图标,做一套式神主题的 PWA 图标
- [ ] 任务拖拽排序(目前是上下移按钮,考虑接入 dnd-kit)
- [ ] 校对默认任务模板,对照当前游戏版本更新/删减副本(悬赏封印次数、斗技规则等经常变)
- [ ] `resetCycle.ts` 补单元测试,覆盖 5:00 边界、周一边界的各种时间点
- [ ] 支持自定义重置时间(不是所有玩家都在国服默认时区)
- [ ] 考虑加一个"导出/导入 JSON"功能,方便换设备时手动搬运数据(不做账号同步的前提下的折中方案)

## 暂不考虑(除非需求变化)

- 多设备云同步 / 账号系统(会引入服务器成本,与项目定位冲突)
- 原生 Android/iOS App(如需要,优先考虑用 Capacitor 包装现有代码,而不是重写)
